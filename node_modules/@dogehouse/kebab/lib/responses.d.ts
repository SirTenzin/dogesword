import { Room, ScheduledRoom } from "./entities";
export declare type GetTopPublicRoomsResponse = {
    rooms: Room[];
    nextCursor: number | null;
};
export declare type GetScheduledRoomsResponse = {
    nextCursor: number | null;
    scheduledRooms: ScheduledRoom[];
};
